//query to create user tables in mysql

use tedit;


create table users ( 
   id int auto_increment primary key ,
   name varchar(255),
   mail varchar(255),
   role_id int,
   FOREIGN KEY (role_id)  REFERENCES roles (id),
   created_at timestamp default current_timestamp,
   updated_at timestamp default current_timestamp);

	
    ------------------------------------------------------------

    query to create roles tables in mysql

    Create table roles (
    id int primary key auto_increment,
    role varchar(255),
    created_at timestamp default current_timestamp ,
    updated_at timestamp default current_timestamp);

--------------------------------------------------------------------------------
   query to create products table 
   
           create table products (id int auto_increment primary key, 
						product_code int,
                        productName int,
                        unit varchar(255),
                        category varchar(255),
                        price int,
                        tax int,
                        hsnCode int,
                        shelf_life varchar(255),
                        user_id int,
                        foreign key (user_id) references users(id),
                        create_at timestamp default current_timestamp , 
                        updated_at timestamp default current_timestamp )



insert into products ( product ,user_id ) values ('Powder	' , 1);                        


   <div data-mdb-input-init class="form-outline mb-4">
                  <select class="form-select" aria-label="Default select example">
                    <option selected></option>
                    <option value="user">User</option>
                    <option value="admin">Admin</option>
                  </select>
                  <label class="form-label" for="form3Example4">Role</label>
                </div>